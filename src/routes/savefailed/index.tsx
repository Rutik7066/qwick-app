import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div class='flex justify-center  items-center min-w-full min-h-screen bg-red-600'>
      <h1 class='text-white text-2xl'>Failed to save folder</h1>
    </div>
  );
});
