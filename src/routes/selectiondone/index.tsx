import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div class='flex justify-center  items-center min-w-full min-h-screen bg-green-600'>
      <h1 class='text-white text-2xl'>Selection done successfully !</h1>
    </div>
  );
});
